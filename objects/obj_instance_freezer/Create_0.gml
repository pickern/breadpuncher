// created per frozen instance (for things like hitstop) to manage freezing and unfreezing the individual instance
frozen_instance = 0; //instance to be frozen
freeze_duration = 10; //duration of freeze
freeze_timer = 0; // timer for tracking freeze
